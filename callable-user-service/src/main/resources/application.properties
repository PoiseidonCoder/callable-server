spring.application.name=callable-user-service
# ===============================
# SERVER
# ===============================
server.port=5000
# ===============================
# DATASOURCE
# ===============================
spring.datasource.url=jdbc:postgresql://ep-wandering-paper-a1fdvcry-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_G0bvmhyzfT8F
spring.datasource.driver-class-name=org.postgresql.Driver
spring.mvc.servlet.path=/api
# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# ===============================
# HIKARI (POOL)
# ===============================
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
# ===============================
# Secret
# ===============================
jwt.secret=${JWT_SECRET}
jwt.accessExpirationMs=${JWT_ACCESS_EXPIRATION_MS}
jwt.refreshExpirationMs=${JWT_REFRESH_EXPIRATION_MS}
image-kit.url-endpoint=${IMAGEKIT_URL_ENDPOINT}
image-kit.private-key=${IMAGEKIT_PRIVATE_KEY}
image-kit.public-key=${IMAGEKIT_PUBLIC_KEY}
google.client-id=${GOOGLE_CLIENT_ID}